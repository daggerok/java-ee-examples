version: "2.1"

services:

  ear-example:
    restart: unless-stopped
    build: .
    volumes: ["ear-example-data:/home/app"]
    ports:
      - "5005:5005"
      - "8080:8080"
      - "8081:8081"
      - "9990:9990"
    networks: [backing-services]

volumes:
  ear-example-data: {}

networks:
  backing-services:
    driver: bridge
